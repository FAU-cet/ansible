---
-   name: setup docker on Head
    hosts: all

    roles:
    -   role: install-docker
    -   role: enable-autostart
        vars:
            services:
            -   docker
    -   role: configure-docker